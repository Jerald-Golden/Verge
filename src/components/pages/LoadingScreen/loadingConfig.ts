/** Minimum time the loading screen stays visible (avoids a flash on fast loads). */
export const MIN_LOADING_MS = 5000

/** Shown in order on the secondary line while assets prepare. */
export const LOADING_LABELS = ['Terrain', 'Shaders', 'Controls', 'Climate', 'Environment'] as const
